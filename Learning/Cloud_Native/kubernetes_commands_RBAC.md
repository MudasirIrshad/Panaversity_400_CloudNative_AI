1. kubectl api-resources
2. kubectl get node / kubectl get nodes
3. kubectl get nodes -o wide
4. kubectl config current-context <!-- This is a master user with all permission of authorization this is only applicable in dev machine in industry not in real production, if i am wrong make it correct. -->

5. kubectl explain Role
<!-- Explain what is kind? what kinds are available few example like Role etc along with their purpose -->

6. kubectl apply -f role.yaml
7. kubectl get role -n taskmanagement